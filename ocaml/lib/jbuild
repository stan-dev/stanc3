(library
 ((name stanclib)
  (libraries (core_kernel))
  (inline_tests)
  (preprocess (pps (ppx_jane ppx_inline_test ppx_expect
                    ;;must go last
                    ppx_driver.runner)))
))

(ocamllex (lexer))

(menhir ((modules (parser))))

(jbuild_version 1)
