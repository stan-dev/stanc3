use ast::Expr;

// Try both immutable "return new AST," and
pub fn transform_ast(e: &Expr) -> Box<Expr> {

}

// a mutable version where the AST is modified:
pub fn transform_ast_mut(e: Box<Expr>) -> () {

}
