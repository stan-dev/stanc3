(executable
 (name stanc)
 (libraries stan_mir stan_frontend stan_math_backend )
 (public_name stanc)
 (preprocess (pps ppx_jane))
 )

(env
 (static
  (flags (-ccopt -static)))
  (dev
  (flags (:standard -g))))