(executable
 (name stanc)
 (libraries frontend stan_math_backend analysis_and_optimization)
 (public_name stanc)
 (preprocess (pps ppx_jane))
 )

(env
 (static
  (flags (-ccopt -static)))
  (dev
  (flags (:standard -g))))