  $ stanc bad_gamma_var.stan
error: Ill-typed arguments supplied to function "hmm_hidden_state_prob".
    ┌─ bad_gamma_var.stan:19:10
 18 │    matrix[K, N + 1] prob;
 19 │    prob = hmm_hidden_state_prob(log_omega, Gamma_v, rho);
    │           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Function called here.
 20 │  }
    = Supplied arguments:
      (matrix, matrix, vector)
    = Available signatures:
      (data matrix, data matrix, data vector) => matrix
        The second argument must be data-only.
        (Local variables are assumed to depend on parameters; same goes for
        function inputs unless they are marked with the keyword "data".)
[exit 1]
  $ stanc bad_log_omega_var.stan
error: Ill-typed arguments supplied to function "hmm_hidden_state_prob".
    ┌─ bad_log_omega_var.stan:19:10
 18 │    matrix[K, N + 1] prob;
 19 │    prob = hmm_hidden_state_prob(log_omega_v, Gamma, rho);
    │           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Function called here.
 20 │  }
    = Supplied arguments:
      (matrix, matrix, vector)
    = Available signatures:
      (data matrix, data matrix, data vector) => matrix
        The first argument must be data-only.
        (Local variables are assumed to depend on parameters; same goes for
        function inputs unless they are marked with the keyword "data".)
[exit 1]
  $ stanc bad_rho_var.stan
error: Ill-typed arguments supplied to function "hmm_hidden_state_prob".
    ┌─ bad_rho_var.stan:19:10
 18 │    matrix[K, N + 1] prob;
 19 │    prob = hmm_hidden_state_prob(log_omega, Gamma, rho_v);
    │           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Function called here.
 20 │  }
    = Supplied arguments:
      (matrix, matrix, vector)
    = Available signatures:
      (data matrix, data matrix, data vector) => matrix
        The third argument must be data-only.
        (Local variables are assumed to depend on parameters; same goes for
        function inputs unless they are marked with the keyword "data".)
[exit 1]
