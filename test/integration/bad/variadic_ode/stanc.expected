  $ stanc bad_abs_tol.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_abs_tol.stan:30:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 31 │              a, 1e-3, 500,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, array[] real, real, int, real,
       real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_initial_state.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_initial_state.stan:29:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 28 │    array[N, 2] real z
 29 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 30 │              1e-5, 1e-3, 500,
    = Supplied arguments:
      (<F1>, real, real, array[] real, real, real, int, real, real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_initial_time.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_initial_time.stan:30:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf_tol(dz_dt, z_init, t, ts,
    │                  ^^^^^ Function called here.
 31 │              1e-5, 1e-3, 500,
    = Supplied arguments:
      (<F1>, array[] real, vector, array[] real, real, real, int, real, real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_max_num_steps.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_max_num_steps.stan:30:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 31 │              1e-5, 1e-3, a,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, real, real, array[] real,
       real, real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_no_args.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_no_args.stan:25:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z) {     // system state {prey, predator}) {
    ⋯  
 24 │    array[N, 2] real z
 25 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 26 │              1e-5, 1e-3, 500);
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, real, real, int)
      where F1 = (real, array[] real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_non_matching_args.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_non_matching_args.stan:29:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 28 │    array[N, 2] real z
 29 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 30 │              1e-5, 1e-3, 500,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, real, real, int, real, real)
      where F1 = (real, array[] real, real, array[] real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, array[] real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_one_arg.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_one_arg.stan:27:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 26 │    array[N, 2] real z
 27 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 28 │              1e-5, 1e-3, 500,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, real, real, int, real)
      where F1 = (real, array[] real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_rel_tol.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_rel_tol.stan:30:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 31 │              1e-5, a, 500,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] real, real, array[] real, int, real,
       real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_times.stan
error: Ill-typed arguments supplied to function "ode_bdf".
    ┌─ bad_times.stan:30:13
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf(dz_dt, z_init, 0.0, ts,
    │              ^^^^^ Function called here.
 31 │              alpha, beta);
    = Supplied arguments:
      (<F1>, array[] real, real, array[] vector, real, real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
  $ stanc bad_times_tol.stan
error: Ill-typed arguments supplied to function "ode_bdf_tol".
    ┌─ bad_times_tol.stan:30:17
  1 │  functions {
  2 │    array[] real dz_dt(real t,       // time
    │                 ----- Callback defined here.
  3 │                 array[] real z,     // system state {prey, predator}
    ⋯  
 29 │    array[N, 2] real z
 30 │    = ode_bdf_tol(dz_dt, z_init, 0.0, ts,
    │                  ^^^^^ Function called here.
 31 │              1e-5, 1e-3, 500,
    = Supplied arguments:
      (<F1>, array[] real, real, array[] vector, real, real, int, real, real)
      where F1 = (real, array[] real, real, real) => array[] real
    = Available signatures:
      (<F2>, vector, real, array[] real, data real, data real, data int) => vector
      where F2 = (real, vector) => vector
        The first argument must be
         (real, vector) => vector
        but got
         (real, array[] real, real, real) => array[] real
        These are not compatible because:
          The types for the second argument are incompatible: one is
           array[] real
          but the other is
           vector
[exit 1]
